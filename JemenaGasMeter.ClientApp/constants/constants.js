// Use this url to connect the client app with the API running on localhost.
// const base_url = "http://127.0.0.1:19000/";

// Use this url to connect the client app with the API running on azure app service.
const base_url = "https://jgm.azurewebsites.net/";

export default base_url
